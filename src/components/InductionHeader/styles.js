import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  logo: {
    width: 65,
    height: 65,
    marginTop: 50,
    alignSelf: 'center',
    marginBottom: 30
  }
});

export default styles;