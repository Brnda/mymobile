import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
container: {backgroundColor: 'blue', flex: 1, margin: 20, alignItems: 'center'},
  text: {},
  textView: {},
});

export default styles;